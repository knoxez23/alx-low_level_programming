#include "main.h"

/**
  * print_sign - Prints sing
  * @m: number
  *
  * Return: 1 positive, -1 for negative or zero elsewhere
  */
int print_sign(int m)
{
	if (n > 0)
	{
		_putchar(43);
		return (1);
	}
	else if (n < 0)
	{
		_putchar(45);
		return (-1);
	}
	else
	{
		_putchar(48);
		return (0);
	}
}
