#include "main.h"

/**
 * _islower - Checks if chareacter is lowcase
 * @l: Character
 *
 * Return: 0 if not lower case 1 otherwise
 */
int _islower(intl)
{
	if (l >= 97 && l <= 122)
	{
		return (1);
	}

	return (0);
}
