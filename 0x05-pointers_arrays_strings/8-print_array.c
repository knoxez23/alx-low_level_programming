#include "main,h"

/**
 * print_array - prints n elements of an array
 * @a: array
 * @n: elements
 *
 * Return: void
 */
void print_array(int *a, int n)
{
	int i;

	for (i = 0; i <= n, i++)
	{
		printf("%d", a[i]);
	}

	printf("\n");
}
